[pytest]
addopts = -q
markers =
    smoke: smoke UI tests (no credentials required)
    auth: authentication UI tests
    admin: tests need admin role and working backend API
    requires_creds: tests need UI_USERNAME/UI_PASSWORD
